# 开发记录


## 组件开发

### log组件的开发

这个log组件使用了环形缓冲区。然后这里的环形缓冲区其实是从zephry源码中剥离出来的，去除了一些私有的宏定义和没必要的功能。这里使用了申请的模式，可以避免复制，这种方法的效率很高。

### 环形缓冲区组件的开发

环形缓冲区从zephry源码中剥离出来的，去除了一些私有定义，修改为了适合裸机使用的代码。对于环形缓冲区的操作主要是提供了两种机制，一种就是很朴素的将字节内容复制进去，然后获取的时候从缓冲区中复制出来，内部会主动处理回绕问题。第二种机制是直接获得环形缓冲区内部的指针，然后基于这个指针进行操作，比如我需要写进去，我会写一块连续的地址，但是环形缓冲区并不知道写了多少，需要调用另外一个函数通知环形缓冲区我写了多少，读也是同理的，我外界只管读，但是环形缓冲区并不知道读取了多少字节，需要调用另外一个函数告知环形缓冲区读取了多少字节，然后更新头尾指针。

第一种方法很直接，也很直观，可以自动处理回绕问题，第二种方法无法处理回绕问题，如果出现了跨越的问题，需要申请两个，第二种方法避免了复制，提高了效率。

### 状态机组件的开发

状态机也是从zephry中剥离出来的。

### 内存管理

内存管理是从FreeRTOS中剥离出来的。

## 核心开发

核心其实很简单，使用一个数组管理所有的任务，然后每次轮询整个数据，看看有没有任务需要执行，任务是否到了预定时间。暂时没有实现其他机制。总的来说还是比较简单的，但是即使是对于这种程度的多任务来说，同步也是非常重要的，虽然可以不开发其他的的东西，但是同步机制，不管怎么说都还是需要的，但是同步机制可以怎么实现呢？还有就是为了保护全局变量或者共有资源等，互斥锁也还是很重要的，而且在使用中经常发现，有时候是需要递归互斥锁，因为在保护资源的时候，难以避免的间接在保护内部有调用了锁。但是因为这里没有考虑到多任务优先级的问题，所以不用担心任务之间抢占的事情，但是中断的优先级是最高的，所以无法避免的就是中断会打断任务，这点是无可避免的。

所以最关键的就是同步，主要是队列，还有锁


## 后续计划

使用链表管理任务，这样可以实现更加灵活的任务动态管理。

添加多任务同步机制，主要是队列机制

添加任务优先级机制，这个其实在链表机制实现之后就可以很容易实现的。就是按照优先级顺序插入删除就可以了。

添加空闲任务，这个的初衷就是为了低功耗设计的

添加低功耗管理，因为使用了裸机，所以对于低功耗的管理更加自由，FreeRTOS对于低功耗只是提供了睡眠模式，但是没有停止模式和待机模式，虽然只要愿意也不是不可以。这么看来这种裸机多任务好像没有什么优势，至于他提升的那一点点效率，对于现在的单片机来说完全不是问题。

## 闲言

其实写着写着发现，还不如直接使用RTOS机制来的更加实在。两个原因，如果一个项目要求使用裸机，个人觉得可能是几个情况，低功耗需求，因为裸机对于低功耗的管理更加自由；第二，成本需求，必须要使用性能被极致压缩的，就是那种刚好可以胜任然后留出一部分余裕的芯片。

针对第一个如果是寻求极致低功耗的话，那么这个功能一般不会非常复杂