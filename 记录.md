# 开发记录


## 组件开发

### log组件的开发

这个log组件使用了环形缓冲区。然后这里的环形缓冲区其实是从zephry源码中剥离出来的，去除了一些私有的宏定义和没必要的功能。这里使用了申请的模式，可以避免复制，这种方法的效率很高。

### 环形缓冲区组件的开发

环形缓冲区从zephry源码中剥离出来的，去除了一些私有定义，修改为了适合裸机使用的代码。对于环形缓冲区的操作主要是提供了两种机制，一种就是很朴素的将字节内容复制进去，然后获取的时候从缓冲区中复制出来，内部会主动处理回绕问题。第二种机制是直接获得环形缓冲区内部的指针，然后基于这个指针进行操作，比如我需要写进去，我会写一块连续的地址，但是环形缓冲区并不知道写了多少，需要调用另外一个函数通知环形缓冲区我写了多少，读也是同理的，我外界只管读，但是环形缓冲区并不知道读取了多少字节，需要调用另外一个函数告知环形缓冲区读取了多少字节，然后更新头尾指针。

第一种方法很直接，也很直观，可以自动处理回绕问题，第二种方法无法处理回绕问题，如果出现了跨越的问题，需要申请两个，第二种方法避免了复制，提高了效率。

### 状态机组件的开发

状态机也是从zephry中剥离出来的。

## 核心开发


## 